<odoo>
    <data>
        <record id="action_appolo_res_partner_wizard" model="ir.actions.server">
            <field name="name">Send to Apollo</field>
            <field name="groups_id" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="binding_model_id" ref="base.model_res_partner"/>
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code">
                if records:
                    action = records.action_send_to_apollo_data()
            </field>
        </record>
        
        <record id="view_partner_form_inherit" model="ir.ui.view">
            <field name="name">view.partner.form.inherit</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <xpath expr="//group[1]/group[1]" position="inside">
                    <field name="founded_year" placeholder="e.g 2001" invisible="company_type != 'company'"/>
                </xpath>
                <xpath expr="//notebook" position="inside">
                    <page string="Apollo">
                        <group>
                            <group>
                                <field name="apl_id" />
                                <field name="update_required_for_apollo" />
                                <field name="apl_date_sync" />
                            </group>
                            <group string="Social Accounts">
                                <field name="linkedin_url" invisible="company_type  != 'company'"/>
                                <field name="twitter_url" invisible="company_type  != 'company'"/>
                                <field name="github_url" invisible="company_type  != 'company'"/>
                                <field name="facebook_url" invisible="company_type  != 'company'"/>
                                <field name="blog_url" invisible="company_type  != 'company'"/>
                                <field name="angellist_url" invisible="company_type  != 'company'"/>
                            </group>
                        </group>
                    </page>
                </xpath>
            </field>
        </record>
        
    </data>
</odoo>