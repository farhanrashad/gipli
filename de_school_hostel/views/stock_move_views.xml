<odoo>

    <record id="hostel_stock_move_line_tree_view" model="ir.ui.view">
        <field name="name">hostel.stock.move.line.tree</field>
        <field name="model">stock.move.line</field>
        <field name="arch" type="xml">
            <tree string="Move Lines" >
                <field name="location_dest_usage" column_invisible="1" />
                <field name="location_usage" column_invisible="1" />
                <field name="room_assign_id" column_invisible="1"/>
                <field name="date"/>
                <field name="reference" string="Reference"
                       column_invisible="context.get('no_reference', False)"/>
                <field name="product_id" string="Unit"/>
                <field name="lot_id" string="Sub Unit" groups="stock.group_production_lot"/>
                <field name="location_id" optional="hide"/>
                <field name="location_dest_id"/>
                <field name="company_id" optional="hide" groups="base.group_multi_company" force_save="1"/>
                <field name="quantity" string="Quantity"
                    decoration-danger="(location_usage in ('internal','transit')) and (location_dest_usage not in ('internal','transit'))"
                    decoration-success="(location_usage not in ('internal','transit')) and (location_dest_usage in ('internal','transit'))"/>
                <field name="product_uom_id" options="{'no_open': True, 'no_create': True}" string="Unit" groups="uom.group_uom"/>
                <field name="state" widget='badge' optional="show"
                       decoration-danger="state=='cancel'"
                       decoration-info="state== 'assigned'"
                       decoration-muted="state == 'draft'"
                       decoration-success="state == 'done'"
                       decoration-warning="state not in ('draft','cancel','done','assigned')"/>
            </tree>
        </field>
    </record>
    
    <record id="action_dormitory_stock_move_lines" model="ir.actions.act_window">
        <field name="name">Dormitory</field>
        <field name="res_model">stock.move.line</field>
        <field name="view_id" ref="hostel_stock_move_line_tree_view"/>
    </record>
</odoo>