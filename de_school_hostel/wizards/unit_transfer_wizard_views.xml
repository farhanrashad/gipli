<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <record id="unit_transfer_wizard_form_view" model="ir.ui.view">
            <field name="name">Unit Transfer</field>
            <field name="model">oe.hostel.transfer.unit.wizard</field>
            <field name="arch" type="xml">
                <form string="Unit Transfer">
                    <field name="product_id_tracking" invisible="1"/>
                    <group>
                        <group string="From" name="from_transfer">
                            <field name="room_assign_id" options="{'no_create':True}" readonly="1"/>
                            <field name="src_product_id" />
                            <field name="src_lot_id" />
                        </group>
                        <group string="To" name="to_transfer">
                            <field name="location_id" 
                                    options="{'no_create':True, 'no_edit':True}"
                                />
                            <field name="product_id" 
                                    options="{'no_create':True, 'no_edit':True}"
                                />
                                <field name="lot_id" 
                                    options="{'no_create':True, 'no_edit':True}"
                                    invisible="product_id_tracking != 'serial'"
                                    required="product_id_tracking == 'serial'"
                                />
                        </group>
                    </group>
                    <footer>
                        <button name="action_transfer" id="action_transfer" string="Transfer" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

    <!--
        <record id="action_transfer_unit_wizard" model="ir.actions.act_window">
            <field name="name">Transfer Unit</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">oe.hostel.transfer.unit.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <field name="binding_model_id" ref="product.model_oe_hostel_room_assign" />
            <field name="binding_view_types">list</field>
        </record>
    -->
</odoo>
