<odoo>

    <record id="action_all_loan_requests" model="ir.actions.act_window">
        <field name="name">Loans to review</field>
        <field name="res_model">hr.loan</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="domain">[('state', 'in', ['confirm']), ('loan_type_id', '=', active_id)]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No new approvals to review 
            </p>
        </field>

    </record>

    
    <record id="loan_type_kanban_view" model="ir.ui.view">
        <field name="name">loan.type.kanban_view</field>
        <field name="model">hr.loan.type</field>
        <field name="arch" type="xml">
            <kanban create="false" class="o_modules_kanban">
                <field name="name"/>
                <field name="id"/>
                <field name="image"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_module_vignette">
                            <t t-if="record.id">
                                <img t-att-src="kanban_image('hr.loan.type', 'image', record.id.raw_value)" alt="Approvals Types Image"
                                    class="oe_kanban_avatar float-start me-3" width="64" height="64"/>
                            </t>
                            <div class="oe_module_desc">
                                <h4 class="o_kanban_record_title">
                                    <field name="name"/>
                                </h4>
                                <p class="oe_module_name">
                                    <field name="description"/>
                                </p>
                                <div class="oe_module_action" t-if="!selection_mode">
                                    <button type="object" class="btn btn-primary btn-sm" name="create_request" context="{'category_id':'active_id'}">New Request</button>
                                    <button type="action" class="btn btn-sm btn-secondary float-end" name="%(de_hr_loan.action_all_loan_requests)d" groups="de_hr_loan.group_loan_user">To Review: <field name="request_to_validate_count"/></button>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    
    <record id="action_loan_dashboard" model="ir.actions.act_window">
        <field name="name">Dashboard</field>
        <field name="res_model">hr.loan.type</field>
        <field name="view_mode">kanban</field>
    </record>
    
    <menuitem
        id="hr_loan_type_dashboard"
        parent="menu_loan_management"
        name="New Request"
        action="action_loan_dashboard"
        sequence="1"/>
</odoo>