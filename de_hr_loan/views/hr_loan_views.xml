<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--Loan Tree view-->
    <record id="hr_loan_tree_view" model="ir.ui.view">
        <field name="name">hr.loan.tree</field>
        <field name="model">hr.loan</field>
        <field name="arch" type="xml">
            <tree string="Loan Requests">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="loan_amount"/>
                <field name="date"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!--Loan Form view-->
    <record id="hr_loan_form_view" model="ir.ui.view">
        <field name="name">hr.loan.form</field>
        <field name="model">hr.loan</field>
        <field name="arch" type="xml">
            <form string="Loan Request">
                <header>
                    <button name="compute_installment" type="object" string="Compute Installment" class="oe_highlight"  attrs="{'invisible':[('state','!=','draft')]}"/>
                    <button name="compute_installment" type="object" string="Compute Installment" attrs="{'invisible':[('state','!=','verify')]}"/>

                    <button string="Mark as Draft" name="action_draft" type="object" attrs="{'invisible': [('state', 'not in', ['draft', 'verify'])]}"/>
                    <button name="action_confirm" type="object" string="Confirm" attrs="{'invisible':[('state','!=','draft')]}"/>
                    <button name="action_confirm" type="object" string="Confirm" class="oe_highlight"  attrs="{'invisible':[('state','!=','verify')]}"/>

                    <button string="Approve" name="action_approve" type="object" class="oe_highlight" attrs="{'invisible': [('state', '!=', 'confirm')]}"/>

                    <button name="action_request_move_create"
                            string="Post Journal Entries"
                            type="object"
                            data-hotkey="y"
                            class="oe_highlight o_loan_post"
                            attrs="{'invisible': [('state', '!=', 'validate')]}"
                            groups="account.group_account_invoice"/>




                    <field name="state" widget="statusbar" statusbar_visible="draft,verified,confirm,validate,post,done"/>
                </header>

                <sheet>
                    <field name="company_id" invisible="1" />
                    <div name="button_box" class="oe_button_box">
                            <button name="action_open_account_move"
                                class="oe_stat_button"
                                icon="fa-file-text-o"
                                type="object"
                                attrs="{'invisible': ['|', ('state', 'not in', ['post', 'done']), ('account_move_id', '=', False)]}" groups="account.group_account_invoice">
                                Journal Entry
                            </button>
                            <field name="account_move_id" invisible="1"/>
                            
                        </div>
                    <div class="oe_title">
                        <span class="o_form_label" >Request for Loan </span>
                        <h1 class="d-flex">
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group >
                        <group>
                            <field name="employee_id" context="{'default_company_id': company_id}" widget="many2one_avatar_employee"/>
                            <field name="loan_type_id" force_save="1" options="{'no_create': True, 'no_open': True}"/>

                            <field name="date"/>
                            <div class="o_td_label">
                                <label for="loan_amount" string="Amount" />
                            </div>
                            <div name="loan_amount">
                                <field name="loan_amount" nolabel="1" style="width: 8rem;" />
                                <span class="ml8" ><field name="currency_id" nolabel="1" style="width: 8rem;" /></span>
                            </div>
                            <field name="installment"/>
                            <field name="date_start" />
                        </group>
                        <group>
                            <field name="accounting_date" groups="account.group_account_invoice,account.group_account_readonly" />
                            <field name="journal_id" groups="account.group_account_invoice,account.group_account_readonly" options="{'no_open': True, 'no_create': True}" context="{'default_company_id': company_id}"/>
                             <field name="address_id" invisible="1" context="{'default_company_id': company_id}"/>
                            <field name="account_move_id" invisible="1" />
                            <field name="department_id"/>
                            <field name="job_id"/>
                            <field name="company_id" options="{'no_create': True}" groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <group>
                            <field name="description"/>
                        </group>
                    <notebook>
                        <page string="Installments">
                            <field name="loan_lines">
                                <tree string="Installments" editable="bottom">
                                    <field name="date"/>
                                    <field name="amount"/>
                                    <field name="paid" readonly="1" invisible="1"/>
                                </tree>
                            </field>
                            <group class="oe_subtotal_footer oe_right" colspan="2" >
                                <field name="total_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                <field name="total_paid_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                <field name="balance_amount" class="oe_subtotal_footer_separator" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                            </group>
                            <div class="oe_clear"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <!--loan search view-->
    <record id="view_loan_request_search_form" model="ir.ui.view">
        <field name="name">hr.loan.search.form</field>
        <field name="model">hr.loan</field>
        <field name="arch" type="xml">
           <search string="Loan">
               <filter string="My Requests" name="myrequest" domain="[('employee_id.user_id.id', '=', uid)]" />
                <field name="employee_id" string="Employee"/>
                <field name="department_id" string="Department"/>
                    <filter string="Employee" name="employee_id" context="{'group_by':'employee_id'}"/>
                    <filter string="Department" name="department_id" context="{'group_by':'department_id'}"/>
                    <filter string="Status" name="status" context="{'group_by':'state'}"/>
            </search>
        </field>
    </record>


    <!--loan menu-->
    <menuitem name="Loans &amp; Advances"
        id="menu_hr_loans_and_advances" parent="menu_loan_management"
        sequence="20"/>

    <record id="action_hr_loan_request" model="ir.actions.act_window">
        <field name="name">Request for Loan</field>
        <field name="res_model">hr.loan</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_myrequest': 1}</field>
        <field name="search_view_id" ref="view_loan_request_search_form"/>
         <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to create a new Loan request.
            </p><p>
                Use this menu to create loan requests.
            </p>
        </field>
    </record>

    <menuitem name="Loan"
        id="menu_base_hr_loan_request"
        parent = "menu_loan_management"
        sequence="1"/>

    <menuitem name="All Requests"
        parent="menu_loan_management"
        id="menu_hr_loan_request"
        action="action_hr_loan_request"/>

    <!-- Shortcuts -->
    <record id="act_hr_employee_loan_request" model="ir.actions.act_window">
        <field name="name">Loans</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hr.loan</field>
        <field name="binding_model_id" ref="hr.model_hr_employee"/>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_myloan': 1}</field>
        <field name="view_id" eval="hr_loan_tree_view"/>
    </record>


    <!-- HR employee inherit Loans -->
    <record id="view_employee_form_loan_inherit" model="ir.ui.view">
        <field name="name">hr.employee.loan.form.inherit</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="%(act_hr_employee_loan_request)d"
                    type="action"
                    class="oe_stat_button"
                    icon="fa-calendar"
                    groups="hr.group_hr_manager,hr.group_hr_user">
                    <field name="loan_count" widget="statinfo" string="Loans"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
