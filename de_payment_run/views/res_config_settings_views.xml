<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.account.accountant</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="account.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <block id="analytic" position="after">
                <block id="payment_run" title="Payment Run">
                    <setting id="track_costs_revenues" title="Allows you to use default journal in payment run." groups="account.group_account_invoice" help="The default accounting journal that will use automatic payment run."
                    >
                        <field name="pr_default_journal_id"/>
                    </setting>
                </block>
            </block>
            
        </field>
    </record>
</odoo>