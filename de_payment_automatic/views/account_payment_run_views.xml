<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="account_payment_run_form_view" model="ir.ui.view">
        <field name="name">account.payment.run.form.view</field>
        <field name="model">account.payment.run</field>
        <field name="arch" type="xml">
            <form string="Payment Run" class="oe_form_configuration">
                <field name="partner_type" invisible="1"/>
                <field name="company_id" invisible="1"/>
                <field name="count_proposal" invisible="1" />
                <header>
                    <button name="button_proposal" 
                        string="Proposal" 
                        type="object" 
                        class="oe_highlight"
                        groups="account.group_account_invoice" 
                        data-hotkey="p"        
                        invisible="state != 'draft'"
                    />

                    <button name="button_proposal" 
                        string="Recompute" 
                        type="object" 
                        groups="account.group_account_invoice" 
                        data-hotkey="p"        
                        invisible="state != 'proposal'"
                    />
                    
                    <button name="button_draft" 
                        string="Set to Draft" 
                        type="object" 
                        groups="account.group_account_invoice" 
                        data-hotkey="p"        
                        invisible="state not in ('draft','proposal')"
                    />
                    
                    <button name="button_schedule" 
                        string="Schedule" 
                        type="object" 
                        class="oe_highlight"
                        groups="account.group_account_invoice" 
                        data-hotkey="d"        
                        invisible="state != 'proposal' or run_method != 'scheduled'"
                    />
                    <button name="button_execute" 
                        string="Execute" 
                        type="object" 
                        class="oe_highlight"
                        groups="account.group_account_invoice" 
                        data-hotkey="e"        
                        invisible="state != 'proposal' or run_method != 'now'"
                    />
                    
                    <field name="state" widget="statusbar" statusbar_visible="draft,proposal,scheduled,posted"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button invisible="count_proposal == 0"
                            name="open_payment_proposal"
                            class="oe_stat_button"
                            icon="fa-file-text-o"
                            type="object">
                            <field name="count_proposal" widget="statinfo" string="Proposal"/>
                        </button>

                        <button invisible="count_payments == 0"
                                name="open_payments"
                                class="oe_stat_button"
                                icon="fa-dollar"
                                type="object">
                            <field name="count_payments" widget="statinfo" string="Payments"/>
                        </button>

                        
                    </div>
                    
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g Payment on 01-01-2024" class="d-block"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="payment_type" widget="radio" options="{'horizontal': True}"
                                       readonly="state != 'draft'"/>

                            <field name="run_method" 
                                readonly="state != 'draft'"
                                widget="radio"
                            />
                            
                            <field name="date"
                                required="run_method == 'scheduled'"
                                readonly="state != 'draft' or run_method != 'scheduled'"
                            />
                            <field name="date_next_run"
                                       readonly="state != 'draft'"/>
                        </group>
                        <group>
                            
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                    
                    <notebook>
                        <page name="exclude_partners" string="Exclude Partners">
                            <field name="exclude_partner_ids" readonly="state not in ('draft','proposal')">
                                <tree>
                                    <field name="name"/>  
                                    <field name="email"/> 
                                </tree>
                            </field>
                        </page>
                        <page name="parameters" string="Parameters">
                            <group>
                                <group >
                                    <field name="date_accounting" 
                                        readonly="state not in ('draft','proposal')"
                                        required="state in ('proposal')"
                                    />
                                    <field name="group_payment" 
                                        readonly="state not in ('draft','proposal')"
                                    />
                                </group>
                                
                            </group>
                        </page>
                        <page name="extra_selection" string="Extra Selection">
                            <group>
                                <group >
                                    <field name="date_invoice" readonly="state not in ('draft','proposal')"/>
                                    <field name="date_due_by" readonly="state not in ('draft','proposal')"/>
                                </group>
                            </group>
                            <group >
                                <field name="filter_domain" 
                                    widget="domain" 
                                    string="Apply On" 
                                    options="{'model': 'account.move', 'in_dialog': True}"
                                    readonly="state not in ('draft','proposal')"
                                />
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <!-- Chatter -->
                <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="account_payment_run_tree_view" model="ir.ui.view">
        <field name="name">account.payment.run.tree.view</field>
        <field name="model">account.payment.run</field>
        <field name="arch" type="xml">
            <tree sample="1" >
                <field name="name" />
                <field name="partner_type" />
                <field name="date" />
                <field name="date_accounting" />
                <field name="state" string="Status" 
                    decoration-success="state == 'sale'" 
                    decoration-info="state == 'draft'" 
                    decoration-primary="state == 'sent'" 
                    widget="badge" 
                />
                <field name="company_id" groups="base.group_multi_company"/>
            </tree>
        </field>
    </record>
    
    <record id="action_payable_payment_run" model="ir.actions.act_window">
        <field name="name">Payment Run</field>
        <field name="res_model">account.payment.run</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[
            ('partner_type', '=', 'supplier'),
        ]</field>
        <field name="context">{
            'default_partner_type': 'supplier',
            'default_payment_type': 'outbound',
            }
        </field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a payment run
            </p>
            <p>
                Automated payment run streamlines payment processing by generating and executing payments automatically based on predefined criteria.
            </p>
        </field>
    </record>

    <menuitem name="Payment Run" 
        id="menu_finance_payables_payment_run"
        parent="account.menu_finance_payables" 
        action="action_payable_payment_run"
        sequence="50"
    />
    
</odoo>