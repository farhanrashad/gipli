<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="project.helpdesk.user.target.wizard_wizard_form_view" model="ir.ui.view">
        <field name="name">project.helpdesk.user.target.wizard.wizard.form</field>
        <field name="model">project.helpdesk.user.target.wizard</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <group>
                        <field name="ticket_ids" invisible="1"/>
                        <field name="project_ids" invisible="1"/>
                        <field name="stage_id" options="{'no_edit':True, 'no_create':True}" />
                    </group>
                   <field name="reopen_reason" placeholder="Please explain why you need to reopen the ticket." />
                </group>
                <footer>
                    <button name="action_reopen_tickets" string="Reopen" type="object" class="btn-primary"/>
                    <button special="cancel" string="Cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>


    <record id="action_set_target" model="ir.actions.act_window">
        <field name="name">Set Target</field>
        <field name="res_model">project.ticket.reopen.wizard</field>
        <field name="view_mode">form</field>
    </record>
</odoo>
