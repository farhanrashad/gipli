<?xml version="1.0" encoding='UTF-8'?>
<odoo>

    <record id="model_sale_subscription_order_cancel" model="ir.actions.server">
        <field name="name">Cancel Subscription</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_view_types">form,list</field>
        <field name="state">code</field>
        <field name="code">
records.filtered('subscription_order')._action_subscription_cancel()
        </field>
    </record>


    <record id="model_sale_subscription_order_paused" model="ir.actions.server">
        <field name="name">Pause Subscription</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_view_types">form,list</field>
        <field name="state">code</field>
        <field name="code">
if records:
    records.action_subscription_pause()
        </field>
    </record>

    
</odoo>