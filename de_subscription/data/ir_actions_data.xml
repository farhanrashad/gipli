<?xml version="1.0" encoding='UTF-8'?>
<odoo>

    <record id="model_sale_subscription_order_cancel" model="ir.actions.server">
        <field name="name">Cancel Subscription</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_view_types">form,list</field>
        <field name="state">code</field>
        <field name="code">
records.filtered('subscription_order')._action_cancel()
        </field>
    </record>


    <record id="model_sale_subscription_order_paused" model="ir.actions.server">
        <field name="name">Pause Subscription</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_view_types">form,list</field>
        <field name="state">code</field>
        <field name="code">
if records:
    records.action_subscription_pause()
        </field>
    </record>

    <!-- Cron jobs -->
        <record model="ir.cron" id="account_analytic_cron">
            <field name="name">Sale Subscription: Expire Subscriptions</field>
            <field name="model_id" ref="de_subscription.model_sale_order"/>
            <field name="state">code</field>
            <field name="code">model._cron_expire_subscription_orders()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">weeks</field>
            <field name="numbercall">-1</field>
            <field name="nextcall" eval="(datetime.now() + timedelta(minutes=5)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="doall" eval="False"/>
        </record>

        <record model="ir.cron" id="account_analytic_cron_for_invoice">
            <field name="name">Sale Subscription: generate invoices for subscriptions</field>
            <field name="model_id" ref="de_subscription.model_sale_order"/>
            <field name="state">code</field>
            <field name="code">model._cron_create_subscription_invoice()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="nextcall" eval="(datetime.now() + timedelta(minutes=7)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
</odoo>