<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

    <record id="sale_subscription_close_reason_form_view" model="ir.ui.view">
        <field name="name">sale.subscription.close.reason.form.view</field>
        <field name="model">sale.sub.close.reason</field>
        <field name="arch" type="xml">
            <form string="Close Reason" class="oe_form_configuration">
                <sheet>
                    <group>
                        <group>
                            <field name="name" />
                            <field name="publish_portal" />
                        </group>
                        <group>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
        <!-- Close reasons -->
    <record id="sale_subscription_close_reason_tree_view" model="ir.ui.view">
        <field name="name">sale.sub.close.reason.list</field>
        <field name="model">sale.sub.close.reason</field>
        <field name="arch" type="xml">
            <tree string="Close Reasons" >
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record id="action_sale_subscription_close_reason" model="ir.actions.act_window">
        <field name="name">Close Reasons</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">sale.sub.close.reason</field>
        <field name="view_mode">tree,form</field>
    </record>
        
        <menuitem action="action_sale_subscription_close_reason" name="Close Reasons" id="menu_subscription_config_close_reason" parent="menu_subscription_config" sequence="10"/>
        
    </data>
</odoo>