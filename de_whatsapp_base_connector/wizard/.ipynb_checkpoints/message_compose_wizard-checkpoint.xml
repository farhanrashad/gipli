<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record model="ir.ui.view" id="whatsapp_compose_message_wizard_form">
            <field name="name">whatsapp.compose.message.form</field>
            <field name="model">whatsapp.compose.message</field>
            <field name="groups_id" eval="[(4,ref('base.group_user'))]"/>
            <field name="arch" type="xml">
                <form string="Compose Message">
                    <group>
                        <!-- truly invisible fields for control and options -->
                        <field name="composition_mode" invisible="1"/>
                        <field name="model" invisible="1"/>
                        <field name="res_id" invisible="1"/>
                        <field name="res_ids" invisible="1"/>
                        <field name="is_log" invisible="1"/>
                        <field name="parent_id" invisible="1"/>
                        <field name="active_domain" invisible="1"/>
                         <field class="oe_inline" name="use_active_domain" invisible="1"/>
                        <!-- Various warnings -->

                        <!-- visible wizard -->   
                        <field name="instance_id"  required="True"/>
                        <label for="partner_ids" string="Recipients"  attrs="{'invisible': [('is_log', '=', True)]}" groups="base.group_user" invisible="1"/>
                        <div groups="base.group_user" attrs="{'invisible': [('is_log', '=', True)]}">
                            <span attrs="{'invisible': [('composition_mode', '!=', 'mass_whatsapp')]}">
                                <strong>Whatsapp Message</strong> on
                                <span attrs="{'invisible': [('use_active_domain', '=', True)]}">the selected records</span>
                                <span attrs="{'invisible': [('use_active_domain', '=', False)]}">the current search filter</span>.
                            </span>
                            <span name="document_followers_text" invisible="1">Followers of the document and</span>
                            <field name="partner_ids" widget="many2many_tags_email" placeholder="Add contacts to notify..."
                                context="{'force_email':True, 'show_email':True}"
                                attrs="{'invisible': [('composition_mode', '!=', 'comment')]}"  invisible="1"/>
                        </div>
                        <field name="subject" placeholder="Subject..." required="True"/>
                        

                        <!-- mass post -->
                        <field name="notify"
                            attrs="{'invisible':[('composition_mode', '!=', 'mass_post')]}"/>
                        <!-- mass mailing -->
                        <field name="no_auto_thread" invisible="1"/>
                    </group>
                    <field name="body" options="{'style-inline': true}"/>
                    <group col="4">
                        <field name="attachment_ids" widget="many2many_binary" string="Attach a file" nolabel="1" colspan="2"/>
                        <field name="template_id" options="{'no_create': True}"
                               context="{'default_model': model, 'default_body_html': body, 'default_subject': subject}"/>
                    </group>
                    <footer>
                            <button string="Send Whatsapp"  name="action_send_whatsapp" type="object" class="btn-primary o_mail_send"/>
                        <button string="Cancel" class="btn-secondary" special="cancel" />

                        <button icon="fa-lg fa-save" type="object" name="save_as_template" string="Save as new template"
                                class="float-right btn-secondary" help="Save as a new template"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_whatsapp_compose_message_wizard" model="ir.actions.act_window">
            <field name="name">Compose Message</field>
            <field name="res_model">whatsapp.compose.message</field>
            <field name="binding_model_id" ref="de_whatsapp_base_connector.model_whatsapp_compose_message"/>
            <field name="type">ir.actions.act_window</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
